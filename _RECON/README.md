This directory is reserved for the recon phase of the workflow. It contains the mapping and candidate artifacts that feed into vuln-class skills, as well as any general recon utilities that don't fit in a specific skill. The default workflow is to run `/recon-mapper` to populate this directory with the mapping and candidate files, then have vuln-class skills consume those artifacts for testing.